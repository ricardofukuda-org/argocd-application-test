apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "baseapp.fullname" . }}-test-connection"
  labels:
    {{- include "baseapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "baseapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
